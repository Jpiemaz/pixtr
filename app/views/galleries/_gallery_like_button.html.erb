<% if current_user.liked? gallery %>
    <%= button_to "Unlike", unlike_gallery_path(gallery), method: :delete, remote: true %>
  <% else %>
    <%= button_to "Like", like_gallery_path(gallery), remote: true %>
<% end %>
